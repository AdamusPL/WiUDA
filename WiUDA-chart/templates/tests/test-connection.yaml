apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "WiUDA-chart.fullname" . }}-test-connection"
  labels:
    {{- include "WiUDA-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "WiUDA-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
