apiVersion: v1
kind: Service
metadata:
  name: {{ include "WiUDA-chart.fullname" . }}
  labels:
    {{- include "WiUDA-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "WiUDA-chart.selectorLabels" . | nindent 4 }}
